# S1 最小运行骨架

## 目标

不使用 API Key 和真实医学资料，使用合成订单生成固定结构病例包并保存到本地文件仓库。

## 输入

- 完全合成的病例订单；
- 已冻结的领域 Schema；
- S0 项目与隐私基线。

## 输出

- Python 工程、严格领域模型和文件仓库；
- 固定合成生成工作流；
- CLI 和 FastAPI 接口；
- 跨平台安装、测试、运行入口及双平台 CI；
- 中文 PRD、质量评价方案、发布治理和医学输入指南。

## 可体验入口

```text
python scripts/test.py
python -m medicine_agents demo --order data/samples/orders/synthetic-order.json --data-dir data/runtime
python scripts/run_api.py
```

API 启动后访问 `http://127.0.0.1:8000/docs`。CLI 和 API 使用同一领域模型、生成工作流与文件仓库。

## 验收证据

- 本地 Ruff 与 34 项 pytest 测试通过；
- 初始化脚本完成依赖安装验证；
- CLI 生成合成病例包且 `data/runtime/` 未被 Git 跟踪；
- Windows/macOS CI 已配置运行同一测试入口；
- 最终隐私边界与提交编号在任务 12 记录。

## 当前状态

验收收口中。工程实现已完成，等待最终一致性、隐私边界和 Git 工作区复核后标记完成。