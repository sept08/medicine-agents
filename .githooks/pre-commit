#!/bin/sh
set -eu

if command -v pwsh >/dev/null 2>&1; then
  exec pwsh -NoProfile -File scripts/check-sensitive-content.ps1
fi

exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/check-sensitive-content.ps1

